# Inicializamo a Tupla colocando valores dentro de parênteses
frutas = ("maçã", "uva", "tomate")
print(frutas)

# Sua ordem não pode ser alterada (Cada valor tem seu indice - inicia no 0)
# Permite valore duplicados

#len() - Indica o tamanho da Tupla
#count(valor) - Mostra quantas vezes tal valor aparece na tupla